"""Task 150 — exact source reconstruction of current live graph.

Generated by reports/scripts/live_to_exact_source.py.
This owns the live ONNX graph in Python source; it is a control baseline,
not a semantic optimization.
"""
from onnx import TensorProto, helper
from ._exact import arr_b64, model, tensor


def build(task):
    inits = [
        tensor('neg_one', arr_b64('k05VTVBZAQB2AHsnZGVzY3InOiAnPGk0JywgJ2ZvcnRyYW5fb3JkZXInOiBGYWxzZSwgJ3NoYXBlJzogKCksIH0gICAgICAgICAgICAgICAgICAgICAgICAgICAgICAgICAgICAgICAgICAgICAgICAgICAgICAgICAgICAgIAr/////')),
    ]
    nodes = [
        helper.make_node('ReduceL2', ['input'], ['side_f'], axes=[0, 1, 2, 3], keepdims=0),
        helper.make_node('Cast', ['side_f'], ['side_i8'], to=3),
        helper.make_node('Shrink', ['side_i8'], ['start_i8'], bias=1.0, lambd=0.5),
        helper.make_node('Cast', ['start_i8'], ['range_start'], to=6),
        helper.make_node('Shrink', ['range_start'], ['range_limit'], bias=30.0, lambd=0.5),
        helper.make_node('Range', ['range_start', 'range_limit', 'neg_one'], ['cols']),
        helper.make_node('Gather', ['input', 'cols'], ['output'], axis=3),
    ]
    value_infos = [
        helper.make_tensor_value_info('side_f', 1, []),
        helper.make_tensor_value_info('side_i8', 3, []),
        helper.make_tensor_value_info('start_i8', 3, []),
        helper.make_tensor_value_info('range_start', 6, []),
        helper.make_tensor_value_info('range_limit', 6, []),
        helper.make_tensor_value_info('cols', 6, [30]),
    ]
    return model('task150_live_exact', nodes, inits, output_dtype=1, opset=13, value_infos=value_infos)
