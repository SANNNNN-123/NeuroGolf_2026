"""Task 126 — exact source reconstruction of current live graph.

Generated by reports/scripts/live_to_exact_source.py.
This owns the live ONNX graph in Python source; it is a control baseline,
not a semantic optimization.
"""
from onnx import TensorProto, helper
from ._exact import arr_b64, model, tensor


def build(task):
    inits = [
        tensor('channel_selector', arr_b64('k05VTVBZAQB2AHsnZGVzY3InOiAnPGY0JywgJ2ZvcnRyYW5fb3JkZXInOiBGYWxzZSwgJ3NoYXBlJzogKDEwLCksIH0gICAgICAgICAgICAgICAgICAgICAgICAgICAgICAgICAgICAgICAgICAgICAgICAgICAgICAgICAgIAoAAIA/AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA')),
        tensor('one_f', arr_b64('k05VTVBZAQB2AHsnZGVzY3InOiAnPGY0JywgJ2ZvcnRyYW5fb3JkZXInOiBGYWxzZSwgJ3NoYXBlJzogKCksIH0gICAgICAgICAgICAgICAgICAgICAgICAgICAgICAgICAgICAgICAgICAgICAgICAgICAgICAgICAgICAgIAoAAIA/')),
        tensor('prefix_idx', arr_b64('k05VTVBZAQB2AHsnZGVzY3InOiAnPGk4JywgJ2ZvcnRyYW5fb3JkZXInOiBGYWxzZSwgJ3NoYXBlJzogKDIsIDIpLCB9ICAgICAgICAgICAgICAgICAgICAgICAgICAgICAgICAgICAgICAgICAgICAgICAgICAgICAgICAgIAoAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAEAAAAAAAAAA==')),
    ]
    nodes = [
        helper.make_node('Einsum', ['input', 'channel_selector'], ['column_sums'], equation='nchw,c->nw'),
        helper.make_node('ReduceMax', ['column_sums'], ['height'], keepdims=1),
        helper.make_node('Sub', ['height', 'one_f'], ['bottom_f']),
        helper.make_node('Equal', ['column_sums', 'bottom_f'], ['center_mask']),
        helper.make_node('Cast', ['column_sums'], ['inside_mask'], to=9),
        helper.make_node('Xor', ['inside_mask', 'center_mask'], ['background_mask']),
        helper.make_node('Concat', ['background_mask', 'center_mask'], ['updates_bool'], axis=0),
        helper.make_node('Cast', ['updates_bool'], ['updates'], to=1),
        helper.make_node('Cast', ['bottom_f'], ['bottom_i'], to=7),
        helper.make_node('Concat', ['bottom_i', 'bottom_i'], ['bottom_i2'], axis=0),
        helper.make_node('Concat', ['prefix_idx', 'bottom_i2'], ['indices'], axis=1),
        helper.make_node('ScatterND', ['input', 'indices', 'updates'], ['output']),
    ]
    value_infos = [
        helper.make_tensor_value_info('column_sums', 1, [1, 30]),
        helper.make_tensor_value_info('height', 1, [1, 1]),
        helper.make_tensor_value_info('bottom_f', 1, [1, 1]),
        helper.make_tensor_value_info('center_mask', 9, [1, 30]),
        helper.make_tensor_value_info('inside_mask', 9, [1, 30]),
        helper.make_tensor_value_info('background_mask', 9, [1, 30]),
        helper.make_tensor_value_info('updates_bool', 9, [2, 30]),
        helper.make_tensor_value_info('updates', 1, [2, 30]),
        helper.make_tensor_value_info('bottom_i', 7, [1, 1]),
        helper.make_tensor_value_info('bottom_i2', 7, [2, 1]),
        helper.make_tensor_value_info('indices', 7, [2, 3]),
    ]
    return model('task126_live_exact', nodes, inits, output_dtype=1, opset=16, value_infos=value_infos)
